# Контроллер итерации - Версия 091120251436

## 📖 О проекте

Контроллер итерации для Node-RED 3.02 с пятишаговой логикой выполнения.

**Модули оборудования:**
- ADAM 6717 (Node-RED)
- ADAM 6224
- Weintek CMT-FHDX-820

## 🚀 Быстрый старт

### Импорт в Node-RED

1. Откройте Node-RED
2. Меню → Import → Clipboard
3. Вставьте содержимое файла `091120251436.json`
4. Deploy

### Проверка версии

На веб-панели должно отображаться:
```
Текущая версия: 2025-11-09_1436 (091120251436)
```

## 📁 Файлы проекта

```
091120251436.json                    ← ОСНОВНОЙ ФАЙЛ (загружайте его в Node-RED)
ОТЧЕТ_РЕФАКТОРИНГ_091120251436.md   ← подробный отчет о рефакторинге
CHANGELOG_091120251436.md            ← история изменений
README_091120251436.md               ← этот файл
```

## 🎯 Основные возможности

- ✅ Пятишаговая итерация с настраиваемыми параметрами
- ✅ Веб-интерфейс для управления и мониторинга
- ✅ Графики в реальном времени
- ✅ Экспорт данных в CSV
- ✅ Сохранение/загрузка параметров
- ✅ HTTP API для интеграции
- ✅ Автоматический расчет длительностей

## 🔧 Архитектура (после рефакторинга)

### Модульная структура контроллера

```
Контроллер итерации
├── Модуль управления состоянием (9 функций)
├── Модуль управления таймерами (3 функции)
├── Модуль коммуникации (1 функция)
├── Модуль логики итерации (3 функции)
└── Модуль обработки команд (5 функций)
```

Всего: **21 функция** с полной JSDoc документацией

### Параметры шага

Для каждого из 5 шагов настраиваются:
- **Начальное значение** - стартовая точка
- **Скорость** - задержка между тиками (мс)
- **Шаг** - величина изменения
- **Конечное значение** - точка остановки
- **Пауза** - задержка после завершения шага (мс)

### Глобальные переменные

#### Управление
- `VER` - версия проекта (число)
- `ITERATION_CONTROL` - состояние (0-стоп, 1-старт, 2-рестарт)
- `ITERATION_STEP` - текущий шаг (1-5)
- `ITERATION_VALUE` - текущее значение итерации

#### Параметры шагов (для каждого i от 1 до 5)
- `ITER_START_i` - начальное значение
- `ITER_SPEED_i` - скорость (мс)
- `ITER_STEP_i` - шаг изменения
- `ITER_END_i` - конечное значение
- `ITER_PAUSE_i` - пауза после шага (мс)

#### Время выполнения
- `ITERATION_CYCLE_TIME` - фактическое время полного цикла (мс)
- `ITERATION_CYCLE_TIME_PLAN` - расчётное время цикла (мс)
- `ITER_STEP_TIME_i` - фактическое время шага i (мс)
- `ITER_STEP_TIME_PLAN_i` - расчётное время шага i (мс)

## 📊 Веб-интерфейс

### Секции панели

1. **Версия** - текущая версия проекта
2. **Изменения** - история версий
3. **Управление** - кнопки управления и статус
4. **Шаг 1-5** - формы настройки параметров
5. **Значения** - таблица всех переменных
6. **График** - визуализация итерации
7. **История версий** - детальный changelog

### Кнопки управления

- **Старт** - запуск/возобновление итерации
- **Пауза** - остановка итерации
- **Рестарт** - перезапуск с первого шага
- **Загрузить из файла** - чтение параметров
- **Сохранить в файл** - запись параметров
- **Вывести тренд** - ручная отрисовка графика
- **Экспорт тренда** - выгрузка данных в CSV

## 🌐 HTTP API

### GET /iteration/state

Получить текущее состояние контроллера

**Ответ:**
```json
{
  "version": "2025-11-09_1436",
  "versionDisplay": "091120251436",
  "control": 1,
  "step": 3,
  "value": 250,
  "steps": [
    {
      "index": 1,
      "start": -1000,
      "speed": 100,
      "step": 100,
      "end": 1000,
      "pause": 0
    },
    // ... остальные шаги
  ]
}
```

## 📝 Логика работы

### Цикл итерации

1. **Шаг 1** → Пауза → **Шаг 2** → Пауза → **Шаг 3** → Пауза → **Шаг 4** → Пауза → **Шаг 5** → (возврат к Шагу 1)

### Пропуск шага

Если `speed = 0` или `step = 0`, шаг **пропускается**:
- Значение `ITERATION_VALUE` **не меняется**
- Пауза после шага **не выполняется**
- Переход к следующему шагу

### Направление итерации

Определяется автоматически:
- Если `end > start` → движение вперед (`step` прибавляется)
- Если `end < start` → движение назад (`step` вычитается)

## 💾 Файлы данных

### Параметры
**Путь:** `/data/controller_iteration_params.json`

**Формат:**
```json
{
  "steps": [
    { "start": -1000, "speed": 100, "step": 100, "end": 1000, "pause": 0 },
    { "start": -500, "speed": 150, "step": 50, "end": 500, "pause": 100 },
    { "start": -250, "speed": 200, "step": 25, "end": 250, "pause": 100 },
    { "start": 0, "speed": 250, "step": 50, "end": 500, "pause": 200 },
    { "start": 500, "speed": 300, "step": 100, "end": -500, "pause": 0 }
  ]
}
```

### Экспорт тренда
**Путь:** `/data/trends/trend_YYYY-MM-DDTHH-MM-SS-SSSZ.csv`

**Формат:**
```csv
timestamp;value;step;elapsed_ms
2025-11-09T14:36:00.000Z;-1000;1;0
2025-11-09T14:36:00.100Z;-900;1;100
2025-11-09T14:36:00.200Z;-800;1;200
...
```

## 🔍 Особенности реализации

### Защита от race conditions
- Флаг `processing` предотвращает конфликты таймеров
- Проверка состояния перед каждой операцией
- Очистка таймеров перед установкой новых

### Буфер графика
- Максимальный размер: **2000 точек**
- Автоматическая очистка при переполнении
- Сохранение снимка для экспорта

### Синхронизация состояния
- Состояние контроллера в `context`
- Параметры в `flow`
- Значения в `global`

## 🐛 Отладка

### Проверка версии
```javascript
global.get('VER')  // должно вернуть 91120251436
```

### Проверка параметров
```javascript
flow.get('steps')  // массив из 5 шагов
```

### Проверка состояния
```javascript
global.get('ITERATION_CONTROL')  // 0, 1 или 2
global.get('ITERATION_STEP')     // 1-5
global.get('ITERATION_VALUE')    // текущее значение
```

## 📚 История версий

См. `CHANGELOG_091120251436.md` для полной истории изменений.

**Последние изменения (091120251436):**
- Рефакторинг контроллера (5 модулей)
- Убран хардкод версии
- Добавлены JSDoc комментарии
- Вынесены константы буфера
- Оптимизированы координаты нод

## ⚠️ Важно

1. **Не удаляйте** узел "Инициализация проекта" - это единственный источник версии
2. **Проверяйте** валидность JSON перед импортом
3. **Сохраняйте** резервные копии перед изменениями
4. **Тестируйте** на тестовом окружении перед продакшеном

## 📞 Поддержка

При возникновении проблем:
1. Проверьте CHANGELOG и ОТЧЕТ
2. Сравните с исходной версией
3. Используйте резервную копию

---

**Версия:** 091120251436  
**Дата:** 09.11.2025  
**Статус:** Готово к использованию ✅
